# Kept minimal — application-test.yml (loaded via @ActiveProfiles("test")) holds all test config.
# Spring merges both files; application-test.yml takes precedence on conflicts.
spring:
  jpa:
    open-in-view: false
